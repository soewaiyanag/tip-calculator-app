import clsx from "clsx";

const AppTitle = () => (
  <h1
    className={clsx(
      "text-center font-semibold text-2xl",
      "uppercase tracking-[0.5rem] text-cyan-dark-2"
    )}
  >
    spli
    <br />
    tter
  </h1>
);

export default AppTitle;
